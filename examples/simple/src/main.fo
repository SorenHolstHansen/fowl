use simple.utils.add
use std.io.println

fn main() void {
	let below = 4000000
	let mut sum = 0

	let mut n = 1
	let mut m = 1
	
	for {
		let new = m + n
		if (new >= below) {
			break
		}

		n = m
		m = new

		if (m % 2 == 0) {
			sum = sum + m
		}
	}

	println("sum = {sum}")
}
