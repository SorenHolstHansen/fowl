use simple.utils.add
use std.io.println

fn main() void {
	let n = 10
	let mut sum = 0
	let mut i = 1

	for (i < n) {
		if (i % 3 == 0 or i % 5 == 0) {
			sum = sum + i
		}
		i = i + 1
	}

	println("sum = {sum}")
}
